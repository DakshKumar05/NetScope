# Base URL of the backend API.
# Leave blank in development: vite proxies /api to the backend (see vite.config.ts).
VITE_API_BASE=

# Where the dev-server proxy forwards /api requests.
VITE_API_PROXY=http://127.0.0.1:8000
